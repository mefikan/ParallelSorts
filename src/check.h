#ifndef PARALLELSORTS_CHECK_H
#define PARALLELSORTS_CHECK_H

#include <vector>

bool is_array_sorted(int* array, int n);
bool is_array_sorted(std::vector<int> &array, int n);

#endif //PARALLELSORTS_CHECK_H
